---
description: Connect project to GitHub
---

1. Initialize Git repository
// turbo
2. Stage all files
   `git add .`
3. Commit files
   `git commit -m "Initial commit"`
4. Add GitHub remote (User must provide URL)
   `git remote add origin <YOUR_REPO_URL>`
5. Push to GitHub
   `git push -u origin main`
